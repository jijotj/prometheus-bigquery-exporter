apiVersion: v1
kind: Service
metadata:
  name: {{ include "prometheus-bigquery-exporter.fullname" . }}
  labels:
    {{- include "prometheus-bigquery-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prometheus-bigquery-exporter.selectorLabels" . | nindent 4 }}
